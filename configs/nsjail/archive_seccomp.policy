/* Strict allowlist for 7z and tar archive operations — deny everything except what's needed */
POLICY archive_strict {
  ALLOW {
    /* Essential I/O */
    read, write, pread64, pwrite64, readv, writev,
    open, openat, openat2, creat, close, lseek,

    /* Stat operations */
    newstat, newfstat, newlstat, newfstatat, statx,

    /* Memory management (7z compression can be memory-intensive) */
    mmap, munmap, mremap, mprotect, madvise, brk,

    /* Process lifecycle — fork/vfork needed by dash when tar spawns gzip */
    fork, vfork, clone, clone3, execve,
    exit, exit_group, wait4, waitid,

    /* Signal handling */
    rt_sigaction, rt_sigprocmask, rt_sigreturn, sigaltstack,
    tgkill, tkill, rt_sigsuspend,

    /* File operations */
    access, faccessat, faccessat2, getcwd, chdir, fchdir,
    dup, dup2, dup3, fcntl, ioctl, pipe, pipe2,
    unlink, unlinkat,
    rename, renameat, renameat2,
    ftruncate,
    fchmod, chmod, utimensat,

    /* Directory operations */
    mkdir, mkdirat, getdents, getdents64,

    /* Essential system info */
    getuid, getgid, geteuid, getegid, getpid, getppid, gettid,
    getrlimit, prlimit64, newuname, arch_prctl, set_tid_address,
    prctl, capget,

    /* Time operations */
    clock_gettime, clock_getres, gettimeofday, nanosleep, clock_nanosleep,

    /* Futex for threading (7z uses multi-threaded compression) */
    futex, set_robust_list, get_robust_list,

    /* File metadata */
    readlink, readlinkat, lgetxattr, fgetxattr,

    /* Filesystem info */
    statfs, fstatfs,

    /* Polling/select */
    poll, ppoll, select, pselect6,

    /* Misc */
    getrandom, umask, sched_yield, sched_getaffinity, rseq, sysinfo,

    /* glibc NSS lookups probe socket(); harmless — clone_newnet isolates */
    socket, connect,

    getpeername
  }
}
USE archive_strict DEFAULT KILL
